this file includes the parts where we added the metadata one by one and then sampled from the non-matched.
It's archived (and deleted from the main notebook) since metadata is already being added in one go from the master metadata file.
It's kept in archive because it shows the process of sampling from the non-matched, which has since been moved to a designated script (sample_200_non_matched.R).